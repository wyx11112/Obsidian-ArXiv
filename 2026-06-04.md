# The Case for Text-to-SQL Friendly Logical Database Design

📂 方向：Text-to-SQL / 数据库 Schema 优化
👤 作者：Shi Heng Zhang, Zhengjie Miao, Jiannan Wang et al.
📅 日期：2026-06-04
🔗 PDF：https://arxiv.org/pdf/2606.03145
🔗 ArXiv：https://arxiv.org/abs/2606.03145

💡 **论文总结**：

**1️⃣ 解决的问题**
传统的逻辑数据库设计以「人写 SQL」为优化目标，而当 LLM 作为 Text-to-SQL 消费者时，同样语义正确的 Schema 设计可能导致截然不同的 SQL 生成准确率。论文指出：面向 LLM 友好性的逻辑数据库设计是一个被严重忽视的优化维度——如何在不改变底层语义的前提下调整 Schema，使其对 LLM 更易理解和使用，从而提升 SQL 生成质量。

**2️⃣ 解决思路和核心技术**
论文提出 **LLM-Friendly Logical Database Design** 目标，并设计了三种保持语义不变的 Schema 变换算子：

- **Schema Abstraction (+A)**：通过逻辑视图将高频 JOIN 路径实例化，减少 LLM 在多表 JOIN 时的推理负担；
- **Schema Partitioning (+P)**：基于工作负载的感知划分，剪除与特定问题无关的表和列，压缩上下文冗余；
- **Schema Renaming (+R)**：将晦涩的表名/列名替换为语义描述性标识符，改善列链接（column linking）和谓词构造精度。

三个算子可以任意组合（+A+P+R），当历史问题-SQL对可用时，分区和抽象由历史数据引导；在零样本场景下，重命名直接应用，抽象退化为逐问题的 ad-hoc 变体。在 BIRD-Union 和 Spider-Union 两个基准上，跨多个 Text-to-SQL pipeline 和 LLM 骨干进行了评估，完整组合 +A+P+R 在所有配置下均稳定提升，验证了该方法作为数据库侧优化的实用性。

**3️⃣ 主要实验结果**
在 BIRD-Union 和 Spider-Union 上，执行准确率（Execution Accuracy）最高提升 **4.2%**；完整算子组合 +A+P+R 在所有 pipeline 和模型骨干下均稳定正向，验证了 LLM-Friendly Schema 设计的有效性。

---

# CAPER: Clause-Aligned Process Supervision for Text-to-SQL

📂 方向：Text-to-SQL / 过程监督 / 强化学习
👤 作者：Lujie Ban, Jiasheng Shi, Jinyang Li, Xiaolin Han, Tsz Nam Chan et al.
📅 日期：2026-06-04
🔗 PDF：https://arxiv.org/pdf/2606.03327
🔗 ArXiv：https://arxiv.org/abs/2606.03327

💡 **论文总结**：

**1️⃣ 解决的问题**
现有 Text-to-SQL 系统主要依赖查询级别的执行正确性作为终端奖励信号，无法定位哪个中间 SQL 子句（如 SELECT、WHERE、GROUP BY）导致了错误；而 token 级密集监督又存在语义对齐差、可能惩罚执行等价查询、难以大规模标注等问题。本文要解决的核心问题：**如何提供子句级别的精细过程监督信号，既能定位 SQL 生成的错误根源，又能用于策略优化和候选验证**。

**2️⃣ 解决思路和核心技术**
论文提出 **CAPER（Clause-Aligned Process suPERvision）**框架：

- **子句级监督自动标注**：通过对 SQL 抽象语法树（AST）进行**反事实干预**（counterfactual intervention），自动生成每个子句的正确性标签，无需人工标注；
- **错误根因定位**：利用子句级反事实信号训练一个轻量级 **Clause-PRM（Process Reward Model）**——CAPER-9B，能在子句边界提供精细反馈；
- **双重应用**：CAPER-9B 既用于策略优化（Policy Optimization），也用于候选 SQL 的验证和排序（Candidate Verification），形成完整的训练-推理闭环。

该方法与现有 Text-to-SQL pipeline 正交，可作为通用的奖励建模模块插入。

**3️⃣ 主要实验结果**
在 BIRD 和 Spider 基准上，子句对齐监督使执行准确率（EX）相对 GPT-5.4 提升最高达 **15.3%**；错误定位能力达到 **84.53% Accuracy** 和 **90.60% MRR**（Mean Reciprocal Rank），显著优于基线。

---

# InfoMem: Training Long-Context Memory Agents with Answer-Conditioned Information Gain

📂 方向：LLM Agent / 长上下文记忆 / 强化学习
👤 作者：Tiancheng Han, Yong Li, Wuzhou Yu, Qiaosheng Zhang, Wenqi Shao et al.
📅 日期：2026-06-04
🔗 PDF：https://arxiv.org/pdf/2606.03329
🔗 ArXiv：https://arxiv.org/abs/2606.03329

💡 **论文总结**：

**1️⃣ 解决的问题**
长上下文任务要求 LLM 从大规模文档中识别并保留与回答相关的信息。现有基于 RL 的分块记忆 Agent 存在两类信号缺陷：稀疏的终端答案奖励无法反映中间记忆质量；词法相似度的中间奖励只监督局部文本重叠，并不能评估最终记忆是否真正支持正确答案的生成。核心问题：**如何设计一个能直接评估"最终记忆对回答有多大帮助"的奖励信号？**

**2️⃣ 解决思路和核心技术**
论文提出 **InfoMem** 奖励机制，核心思路是用**答案条件信息增益**来评估最终记忆的效用：

- **信息增益度量**：计算模型在给定最终记忆后，对 ground-truth 答案每个 token 的 log-likelihood 提升量，直接量化记忆的信息有效性；
- **稳定化策略**：InfoMem 仅对成功轨迹（successful trajectories）施加该信号，并在奖励合成前做归一化，以防止 RL 优化不稳定；
- **框架集成**：在相同的 **GRPO 训练框架**和预算下，InfoMem 作为额外奖励项与任务成功奖励组合使用，无需修改 Agent 架构。

消融分析揭示了三条关键设计原则：有效的最终记忆奖励应仅作用于成功轨迹、在合成前归一化、以答案（而非 query）为条件。

**3️⃣ 主要实验结果**
在相同 GRPO 框架和训练预算下，InfoMem 显著优于可比记忆 Agent RL 基线；消融实验验证了答案条件、成功轨迹筛选和归一化三个设计要素各自的独立贡献。论文未公开具体数值指标，但定性对比结果均为正向。
