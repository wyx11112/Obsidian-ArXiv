## FDABench: A Benchmark for Data Agents on Analytical Queries over Heterogeneous Data

📂 方向：数据 Agent 评测 / 异构数据分析
👤 作者：Ziting Wang, Shize Zhang, Haitao Yuan et al.
📅 日期：2026-06-02
🔗 PDF：https://arxiv.org/pdf/2509.02473
🔗 ArXiv：https://arxiv.org/abs/2509.02473

💡 **论文总结**：

**1️⃣ 解决的问题**
现有 Data Agent 评测基准往往只覆盖单一数据模态或孤立的 Agent 能力，无法对跨数据库、文档、网页、图片、视频、音频等**异构数据**的综合分析推理能力进行系统性评估。这导致研究者难以全面衡量不同 Agent 架构在真实复杂数据分析场景下的表现差距。

**2️⃣ 解决思路和核心技术**
论文提出 FDABench，一个面向异构数据分析场景的 Data Agent 综合评测基准，包含三项核心贡献：

1. **大规模多模态任务集**：共 2,007 个任务，涵盖六种数据模态（数据库、文档、网页内容、图片、视频、音频），并配套统一的多粒度评估框架，能在不同抽象层次上衡量 Agent 的推理准确性。

2. **PUDDING 数据集构建框架**：为解决 benchmark 构建成本高、质量难保证的问题，作者设计了 PUDDING（一种 Agentic 数据集构建流水线），结合 LLM 生成与迭代式专家校验，实现可靠、可扩展的高质量数据集自动化生产。

3. **多架构基线实验**：在通用分析 Agent、Semantic Operator 框架和 RAG-based 方法等多类主流 Data Agent 架构上进行了详尽对比实验，总结了各类架构的优劣规律和改进方向，为未来 Data Agent 研发提供了具体的实践指引。

**3️⃣ 主要实验结果**
论文对多类 Data Agent 架构（通用分析 Agent、Semantic Operator、RAG-based 方法）在 2,007 个任务上进行了广泛测评，揭示了各架构在不同模态和任务粒度上的关键表现差异；具体数值指标论文未在 Abstract 中公开披露。

---

## Sophrosyne: Agentic Exploration of Relational Data Systems Needs Moderation

📂 方向：Text2SQL Agent / 关系型数据系统 Agent 探索
👤 作者：Madhav Jivrajani, Ramnatthan Alagappan, Aishwarya Ganesan et al.
📅 日期：2026-06-02
🔗 PDF：https://arxiv.org/pdf/2605.30862
🔗 ArXiv：https://arxiv.org/abs/2605.30862

💡 **论文总结**：

**1️⃣ 解决的问题**
基于 LLM 的 Text2SQL Agent 在生成 SQL 前需要通过 API 调用对数据库 Schema 进行探索，但当前数据系统普遍暴露**细粒度 API**（fine-grained API），导致 Agent 过度探索（over-exploration）——引入大量无关 Schema 元素，最终生成的 SQL 准确性严重下降。如何在探索效率与 SQL 生成准确性之间取得平衡是核心难题。

**2️⃣ 解决思路和核心技术**
论文系统研究并分类了数据系统当前暴露的 API 类型：**粗粒度 API**（coarse-grained）和**细粒度 API**（fine-grained），指出两者在探索效率与 SQL 准确性之间存在根本性权衡——大多数系统选择细粒度 API 以保证访问安全性，但这恰恰使 Agent 陷入过度探索的陷阱。

为此，论文提出 **Sophrosyne**，一个数据系统环境增强方案，核心思路是在 API 响应中**注入引导指令（directives）**，主动约束 Agent 的探索范围，告知其哪些 Schema 元素与当前查询意图相关，从而在不牺牲访问安全性的前提下显著减少无效探索、提升 SQL 生成质量。该方案对数据系统侧的改动最小，仅需在 API 响应层面增加语义引导信息。

**3️⃣ 主要实验结果**
Sophrosyne 将 Agent 的过度探索次数降低 **4.6 倍**，SQL 生成准确率提升最高 **12.4%**（约 4 个百分点）。

---

## AIM: A Practical Approach to Automated Index Management for SQL Databases

📂 方向：数据库自动化索引管理 / 数据库物理设计
👤 作者：Ritwik Yadav, Satyanarayana R. Valluri, Mohamed Zaït et al.
📅 日期：2026-06-02
🔗 PDF：https://arxiv.org/pdf/2605.31406
🔗 ArXiv：https://arxiv.org/abs/2605.31406

💡 **论文总结**：

**1️⃣ 解决的问题**
SQL 数据库的物理设计（尤其是索引选择）对查询性能至关重要，但随着工作负载动态变化，人工维护索引代价高昂且容易滞后。现有自动索引推荐系统在大规模生产环境中面临推荐收敛慢、对查询优化器依赖过重、以及缺乏"无回归"安全保障等挑战。

**2️⃣ 解决思路和核心技术**
论文介绍 **AIM（Automatic Index Manager）**，一个面向大规模生产 SQL 数据库的自动化索引管理系统，端到端覆盖从索引识别到部署的完整流程，具备以下关键设计：

1. **快速收敛推荐**：即使面对宽复合索引（wide composite indexes），也能快速识别高影响力的候选索引，显著缩短推荐收敛时间。

2. **降低优化器依赖**：减少对查询优化器的依赖，避免因优化器估算偏差导致的错误推荐，提升推荐鲁棒性。

3. **"无回归"生产保障**：对生产工作负载提供明确的无性能回归保证，每条索引推荐均附带基于指标驱动的可解释性说明，便于运维人员验证机器决策的合理性。AIM 已在数千个生产数据库上完成验证部署。

**3️⃣ 主要实验结果**
实验结果表明 AIM 能够快速识别最有效的索引组合，最终物理设计接近最优；具体性能提升百分比等量化指标论文未在 Abstract 中公开披露。
