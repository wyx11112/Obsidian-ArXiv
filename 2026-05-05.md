# 每日 ArXiv 论文推送 · 2026-05-05

---

## 论文一

**EGREFINE: An Execution-Grounded Optimization Framework for Text-to-SQL Schema Refinement**

📂 方向：Text-to-SQL / 数据库 Schema 优化
👤 作者：Jiaqian Wang, Yutao Qi, Wenjin Hou et al.
📅 日期：2026-05-04
🔗 PDF：https://arxiv.org/pdf/2605.00628
🔗 ArXiv：https://arxiv.org/abs/2605.00628

💡 **论文总结**：

**1️⃣ 解决的问题**

真实世界数据库 Schema 中普遍存在列名歧义、缩写或命名不一致的问题，这会显著降低 Text-to-SQL 模型的准确率。现有方法将 Schema 视为固定不变的，只在下游错误处理阶段进行修补，而非从根源上解决 Schema 命名质量问题。

**2️⃣ 解决思路和核心技术**

论文将 Schema 优化形式化为一个约束优化问题：寻找一个列重命名函数，在保持查询等价性的前提下，最大化下游 Text-to-SQL 执行准确率。经理论分析证明该问题具有计算难度，因此采用列级贪心分解策略，并将其具体化为 **EGRefine** 四阶段流水线：

1. **筛选阶段（Screen）**：识别语义歧义的列名；
2. **生成阶段（Generate）**：基于上下文为歧义列生成候选新名；
3. **验证阶段（Verify）**：通过执行反馈对候选名进行验证，保守选择规则确保列级不降级；
4. **物化阶段（Materialize）**：以非破坏性 SQL View 的形式落地，保障数据库级查询等价性。

该框架具有两个核心结构性保证：列局部不降级（column-local non-degradation）和数据库级查询等价（database-level query equivalence）。精炼后的 Schema 可跨模型族迁移，实现"一次精炼、多模型复用"的部署效果。

**3️⃣ 主要实验结果**

在受控 Schema 降级基准、真实世界基准及企业基准上，EGRefine 均有效恢复了因 Schema 命名噪声导致的准确率损失；在底层任务超出当前 Text-to-SQL 能力时，系统能正确弃权（abstain）。论文未公开具体数字指标。

---

## 论文二

**E-mem: Multi-agent based Episodic Context Reconstruction for LLM Agent Memory**

📂 方向：Agent Memory / LLM Agent 记忆管理
👤 作者：Kaixiang Wang, Yidan Lin, Jiong Lou et al.
📅 日期：2026-05-04
🔗 PDF：https://arxiv.org/pdf/2601.21714
🔗 ArXiv：https://arxiv.org/abs/2601.21714

💡 **论文总结**：

**1️⃣ 解决的问题**

现有 LLM Agent 记忆机制普遍采用"记忆预处理"范式（如 embedding 或图压缩），将复杂的序列依赖压缩成预定义结构，导致推理所必需的上下文完整性被破坏（去上下文化问题）。这使得 Agent 在需要跨步骤逻辑推理的长时程任务中表现欠佳。

**2️⃣ 解决思路和核心技术**

论文提出 **E-mem** 框架，将范式从"记忆预处理"转向"情节式上下文重构（Episodic Context Reconstruction）"，灵感来源于生物记忆印迹（engram）机制。核心架构为异构层级式多智能体设计：

- **多个助手 Agent（Assistant Agents）**：各自维护未压缩的原始记忆上下文片段，负责在激活的本地片段内进行局部推理，提取上下文感知的证据；
- **中央主 Agent（Master Agent）**：负责全局规划与协调，整合各助手提取的证据完成最终推理。

与被动的向量检索不同，E-mem 赋予助手 Agent 主动推理能力，在片段内部先提取证据再聚合，从而保留了跨步骤的上下文完整性，支持深层推理（System 2 reasoning）。

**3️⃣ 主要实验结果**

在 LoCoMo 基准上，E-mem 实现了超过 **54% F1**，比当前 SOTA 方法 GAM 高出 **7.75%**，同时 token 消耗降低超过 **70%**。

---

## 论文三

**HyMem: Hybrid Memory Architecture with Dynamic Retrieval Scheduling**

📂 方向：Agent Memory / LLM 长期记忆管理
👤 作者：Xiaochen Zhao, Kaikai Wang, Xiaowen Zhang et al.
📅 日期：2026-05-04
🔗 PDF：https://arxiv.org/pdf/2602.13933
🔗 ArXiv：https://arxiv.org/abs/2602.13933

💡 **论文总结**：

**1️⃣ 解决的问题**

LLM Agent 在长对话场景中面临记忆管理的根本性两难困境：记忆压缩虽高效但会丢失复杂推理所需的关键细节；保留原始文本则对简单查询产生不必要的计算开销。现有单体记忆表征与静态检索机制无法适应多样化的查询场景。

**2️⃣ 解决思路和核心技术**

论文提出 **HyMem**，一种基于认知经济性原则（cognitive economy）的混合记忆架构，通过多粒度记忆表征实现动态按需调度：

- **双粒度存储（Dual-Granular Storage）**：同时维护摘要级（summary-level）和原始文本级两种粒度的记忆；
- **动态两层检索系统（Dynamic Two-Tier Retrieval）**：
  - 轻量级模块：构建摘要级上下文，用于高效响应简单查询；
  - LLM-based 深层模块：仅在检测到复杂查询时选择性激活，结合反思机制（reflection mechanism）进行迭代推理精炼；
- 系统动态判断查询复杂度，自动路由到对应检索层，模拟人类灵活的前摄性记忆调度能力。

**3️⃣ 主要实验结果**

在 **LOCOMO** 和 **LongMemEval** 两个基准上，HyMem 超越了全上下文（full-context）基线，同时将计算成本降低 **92.6%**，在长期记忆管理的效率与性能平衡上达到新的 SOTA。
