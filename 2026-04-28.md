# 每日 ArXiv 论文推送 · 2026-04-28

---

## 论文 1

**Memanto: Typed Semantic Memory with Information-Theoretic Retrieval for Long-Horizon Agents**

📂 方向：Agent Memory
👤 作者：Seyed Moein Abtahi, Rasa Rahnema, Hetkumar Patel et al.
📅 日期：2026-04-27
🔗 PDF：https://arxiv.org/pdf/2604.22085
🔗 ArXiv：https://arxiv.org/abs/2604.22085

💡 **论文总结**：

**1️⃣ 解决的问题**
当前 LLM 驱动的 Agent 从无状态推理转向持久化多轮自主 Agent 时，内存管理成为核心架构瓶颈。现有方法主要依赖混合语义图架构（Hybrid Semantic Graph），不仅需要 LLM 介导的实体提取、显式图 Schema 维护和多查询检索管道，还带来巨大的计算开销，严重影响生产环境部署效率。

**2️⃣ 解决思路和核心技术**
本文提出 Memanto，一个面向 Agentic AI 的通用内存层，核心思路是挑战"知识图谱复杂性是实现高保真 Agent 记忆必要条件"这一假设。

Memanto 的架构包含三个关键组件：
- **类型化语义内存模式（Typed Semantic Memory Schema）**：预定义 13 个内存类别，对不同类型的信息进行结构化存储，避免非结构化向量堆积；
- **自动化冲突解决机制（Automated Conflict Resolution）**：检测并消解新旧记忆中的矛盾信息，保持记忆库的一致性；
- **时序版本管理（Temporal Versioning）**：对记忆进行时间戳追踪，支持历史状态溯源。

这三个组件均基于 Moorcheh 信息论搜索引擎实现，该引擎是一个无需预建索引的语义数据库，采用信息论方法实现确定性检索，单次查询延迟低于 90 毫秒，并彻底消除了传统向量数据库的摄取延迟（ingestion delay）。与图数据库和向量数据库需要多查询管道不同，Memanto 仅需单次检索查询即可完成记忆召回。

**3️⃣ 主要实验结果**
在 LongMemEval 和 LoCoMo 两个评估基准上，Memanto 分别取得 89.8% 和 87.1% 的准确率，超越所有参与对比的混合图和向量系统，同时实现零摄取成本和单查询检索。

---

## 论文 2

**AgentSearchBench: A Benchmark for AI Agent Search in the Wild**

📂 方向：AI Agent
👤 作者：Bin Wu, Arastun Mammadli, Xiaoyu Zhang et al.
📅 日期：2026-04-27
🔗 PDF：https://arxiv.org/pdf/2604.22436
🔗 ArXiv：https://arxiv.org/abs/2604.22436

💡 **论文总结**：

**1️⃣ 解决的问题**
随着 AI Agent 生态系统的快速扩张，"如何从海量 Agent 中找到适合特定任务的 Agent"成为新的工程难题。与传统工具不同，Agent 的能力往往是组合型的、执行依赖型的，仅凭文字描述难以评估其实际能力。现有研究通常假设功能明确、候选池受控，缺乏对真实野外场景下 Agent 搜索问题的系统性研究。

**2️⃣ 解决思路和核心技术**
本文提出 AgentSearchBench，一个针对"现实 Agent 搜索"场景的大规模基准测试集，覆盖来自多个提供商的近 10,000 个真实 Agent。

基准将 Agent 搜索形式化为两类问题：**检索（Retrieval）**和**重排序（Reranking）**，并分别在两种查询模式下进行评估：
- **可执行任务查询（Executable Task Queries）**：具体可运行的任务描述；
- **高层任务描述（High-level Task Descriptions）**：模糊的自然语言意图。

评估使用**基于执行结果的相关性信号（Execution-grounded Performance Signals）**而非单纯的语义相似度，更接近真实使用场景。实验发现语义相似度与 Agent 实际执行性能之间存在系统性差距，揭示了纯描述型检索的局限性。此外，研究表明引入轻量级行为信号（包括执行感知探测，Execution-aware Probing）可显著提升 Agent 排序质量，强调将执行信号纳入 Agent 发现流程的重要性。

**3️⃣ 主要实验结果**
论文未公开具体指标数字，但实验一致表明：执行感知行为信号能够显著改善基于描述的检索和重排序方法的排名质量，语义相似度与实际 Agent 性能之间存在明显 gap。

---

## 论文 3

**ResRank: Unifying Retrieval and Listwise Reranking via End-to-End Joint Training with Residual Passage Compression**

📂 方向：RAG / Context Management
👤 作者：Xiaojie Ke, Shuai Zhang, Liansheng Sun et al.
📅 日期：2026-04-27
🔗 PDF：https://arxiv.org/pdf/2604.22180
🔗 ArXiv：https://arxiv.org/abs/2604.22180

💡 **论文总结**：

**1️⃣ 解决的问题**
基于 LLM 的 Listwise 重排序（Listwise Reranking）已成为信息检索的主流范式，但其将全文 Passage 输入 LLM 的方式存在两个关键瓶颈：（1）随输入长度增加，"Lost in the Middle"现象导致排序质量下降；（2）推理延迟随序列长度超线性增长，使其在工业级部署中难以实用。

**2️⃣ 解决思路和核心技术**
ResRank 提出将检索与重排序统一到单一框架中，核心灵感来自多模态 LLM 将视觉输入压缩为紧凑 Token 表示的思路。

具体架构包含：
- **Encoder-LLM**：将每个候选 Passage 压缩为单一嵌入向量（Single Embedding），彻底解决长序列问题；
- **Reranker-LLM**：接收查询文本与压缩后的嵌入，执行 Listwise 排序；
- **残差连接结构（Residual Connection）**：将 Encoder 嵌入与 Reranker 的上下文隐藏状态融合，缓解压缩表示空间与排序空间之间的对齐偏差；
- **余弦相似度评分机制**：替代传统自回归解码，实现一步打分，彻底消除生成瓶颈，每个 Passage 仅需处理 1 个 Token。

训练策略采用**双阶段多任务端到端联合优化**，同步训练 Encoder 和 Reranker，实现检索与重排序的学习目标对齐，并大幅降低训练复杂度。

**3️⃣ 主要实验结果**
在 TREC Deep Learning 和 8 个 BEIR 基准数据集上的大量实验表明，ResRank 在生成零 Token、每 Passage 仅处理 1 个 Token 的条件下，实现了与现有方法相当或更优的排序效果，在效果与效率之间取得更优平衡。
