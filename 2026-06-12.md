## TAHOE: Text-to-SQL with Automated Hint Optimization from Experience

📂 方向：Text-to-SQL / 数据库智能查询
👤 作者：Zhiyi Chen, Jie Song, Peng Li et al.
📅 日期：2026-06-11
🔗 PDF：https://arxiv.org/pdf/2606.12387
🔗 ArXiv：https://arxiv.org/abs/2606.12387

💡 **论文总结**：

**1️⃣ 解决的问题**
现有 Text-to-SQL 系统在从原型到生产部署时面临严重挑战：需要处理严格的 SQL 方言、大规模数据库 Schema 和动态变化的用户偏好。监督微调成本高且缺乏灵活性，基于 Agent 的测试时扩展计算开销大，尤其在真实工业场景（如 Snowflake 等云数仓）下准确率仍然不足。

**2️⃣ 解决思路和核心技术**
TAHOE 将 Prompt 优化问题重新建模为**动态数据管理问题**，其核心是一个错误驱动的 Hint 学习流水线。系统分两阶段运行：开发阶段（Development）和部署阶段（Deployment）。在开发阶段，TAHOE 将调试过程中产生的 trace 信息压缩整合为结构化的 **Hint Bank**，分为两类：**Syntax Hints**（从编译器反馈中提炼，针对方言特定规则）和 **Semantic Hints**（从执行反馈和用户反馈中提取，针对 Schema 及用户特定逻辑）。TAHOE 还引入了**策略层（Strategy Layer）**，将相互冲突的用户意图建模为在相同自然语言触发条件下竞争的策略，并通过时近性信号和后学习归因统计（成功率、危害率、无效率、支持度）动态管理策略。推理时，系统检索相关 Hint 引导 LLM 先进行逻辑规划（Logic Planning）再进行 SQL 合成（SQL Synthesis），避免了模型参数更新。

**3️⃣ 主要实验结果**
在 Spider 2.0-Snow 基准（113 个监督样例，使用 GPT-5.5 骨干）：Pass Rate 从 61.95% 提升至 79.42%（+17.47 pp），Pass@4 从 72.57% 提升至 87.61%，Snowflake 语法通过率达 100%，编译器反馈迭代轮次从 2.79 降至 0.12。Hint Bank 迁移到较弱骨干 Doubao-2.0-lite 也带来 +19.7 pp 的 Pass Rate 提升。

---

## Organize then Retrieve: Hierarchical Memory Navigation for Efficient Agents

📂 方向：LLM Agent 记忆管理 / 高效上下文检索
👤 作者：Hao-Lun Hsu, Nikki Lijing Kuang, Boyi Liu, Zhewei Yao, Yuxiong He et al.
📅 日期：2026-06-11
🔗 PDF：https://arxiv.org/pdf/2606.11680
🔗 ArXiv：https://arxiv.org/abs/2606.11680

💡 **论文总结**：

**1️⃣ 解决的问题**
LLM Agent 在执行长时域任务时天然无状态，需要将所有任务相关信息编码进不断增长的输入上下文中，导致推理质量下降、推理成本升高、延迟增大。现有方法要么依赖有损压缩（丢失细节），要么依赖相似度检索（无法捕获时序结构和因果依赖），难以满足多步 Agent 任务的需求。

**2️⃣ 解决思路和核心技术**
HORMA（Hierarchical Organize-and-Retrieve Memory Agent）将 Agent 的历史经验组织成**文件系统式的层级结构**，其中摘要化的实体节点链接到对应的原始轨迹，实现高效访问的同时保留细节信息。系统的工作记忆分为两个阶段：**结构化记忆构建**和**导航式检索**。构建模块通过区分"因信息缺失导致的失败"和"因信息误导或过载导致的失败"来迭代优化经验的组织方式，使记忆结构动态自我修正。检索模块由一个用强化学习训练的轻量级 Agent 负责，通过遍历层级结构选取最小且充分的上下文，直接降低关键执行路径上的延迟，而非全量加载历史。

**3️⃣ 主要实验结果**
在 ALFWorld、LoCoMo 和 LongMemEval 三个基准上，HORMA 在受限上下文预算下提升了任务完成率，在长对话任务中最多仅需基线 **22.17%** 的 Token 用量即可达到更优或相当的性能。相比现有方法，在效率-性能权衡上持续领先，且能有效泛化到未见过的任务。

---

## Layer-Isolated Evaluation: Gating the Deterministic Scaffold of a Production LLM Agent

📂 方向：LLM Agent 评测 / 生产系统质量保障
👤 作者：Sawyer Zhang, Alexander Wang, Sophie Lei et al.
📅 日期：2026-06-11
🔗 PDF：https://arxiv.org/pdf/2606.11686
🔗 ArXiv：https://arxiv.org/abs/2606.11686

💡 **论文总结**：

**1️⃣ 解决的问题**
当前 LLM Agent 的评测主要依赖端到端任务成功率这一聚合指标，但聚合指标只能告诉你系统退化了，却无法定位退化发生在哪一层。当某一层出现局部缺陷时，整体 Pass Rate 的变化往往极为微弱（因其他层掩盖），导致问题难以被发现和定位。

**2️⃣ 解决思路和核心技术**
论文提出**层隔离评测（Layer-Isolated Evaluation）**框架：将一个生产 Agent（订单处理 Agent）拆解为固定的层级分类体系（本体层、意图层、路由层、分解层、上报层、安全层、记忆层及跨层信封/防御层），每层由独立的断言切片（assertion slice）以纯确定性、**无需调用 LLM 的 "pure" 模式**进行测试。整个测试套件包含 238 个测试用例，覆盖 23 个切片，其中 225 个在 2.39 秒内完成（约 10ms/用例），可无缝集成到 CI 流水线并在每次变更时运行。系统采用基于 CheckList 精神的回归锁定基线（regression-locked baseline），每个切片有独立的通过率基线，一旦对应层发生退化，该切片指标会急剧下降。

**3️⃣ 主要实验结果**
通过受控的单层回归注入实验（7 个非安全层各注入一次），聚合指标变化仅 -1.7 至 -5.9 pp（严重掩盖），而对应切片指标下降 -25 至 -91 pp。注入层的切片在 7/7 情况下均进入 Top-3 最差切片，5/7 情况下为最差切片（平均排名 1.29/19）。该结果在第二个结构不同的租户（Starbucks SG）上完全复现，验证了方法的泛化性。
