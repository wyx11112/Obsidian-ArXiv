## UniQL: Towards Dialect-Universal Benchmarking for Text-to-SQL

📂 方向：Text-to-SQL / 数据库方言泛化
👤 作者：Jianling Gao, Chongyang Tao, Jiayuan Bai et al.
📅 日期：2026-06-09
🔗 PDF：https://arxiv.org/pdf/2606.08018
🔗 ArXiv：https://arxiv.org/abs/2606.08018

💡 **论文总结**：

**1️⃣ 解决的问题**
现有 Text-to-SQL 基准测试几乎全部基于 SQLite，导致难以评估模型在真实生产环境中跨异构数据库方言的泛化能力。实际数据库系统（如 PostgreSQL、MySQL、Spark SQL 等）在语法、函数、类型系统和执行语义上差异显著，同一自然语言意图往往需要方言特定的 SQL 实现，但现有评估体系对此几乎没有覆盖。

**2️⃣ 解决思路和核心技术**
论文提出 UniQL，一个人工验证的跨方言 Text-to-SQL 基准。核心构建思路是：将 1,534 个自然语言问题与 16 种 SQL 方言的可执行 SQL 注释对齐，最终生成 24,544 条方言特定查询。所有方言共享相同的意图、对齐的 schema 和数据库内容，从而支持对方言泛化能力的受控对比评估。构建流程采用混合 pipeline，包含：数据库迁移（将 SQLite schema 移植到 16 种目标数据库）、SQL 翻译（方言自动转换）、执行引导验证（确保语义等价性）、迭代规则总结（归纳方言转换规律）以及人工验证（保证标注质量）。实验覆盖开源与闭源大型语言模型，在 16 种方言上全面测试了当前 SOTA 模型的跨方言泛化表现。

**3️⃣ 主要实验结果**
实验表明当前模型距离方言通用（dialect-universal）仍有显著差距：不同数据库系统间性能差异明显，且在 SQLite 上取得的高分难以迁移到其他方言，揭示了现有模型的方言感知能力严重不足。代码和数据已开源（https://github.com/JerryGao818/UniQL）。

---

## Anything2Skill: Compiling External Knowledge into Reusable Skills for Agents

📂 方向：RAG 增强 Agent / 程序性知识编译
👤 作者：Qianjun Pan, Yutao Yang, Junsong Li et al.
📅 日期：2026-06-09
🔗 PDF：https://arxiv.org/pdf/2606.09316
🔗 ArXiv：https://arxiv.org/abs/2606.09316

💡 **论文总结**：

**1️⃣ 解决的问题**
RAG 方法让 Agent 能在推理时访问外部知识，但其检索到的往往是碎片化的陈述性证据（passages、manuals），Agent 需要反复从这些材料中推断任务执行流程，既低效又不稳定。核心问题在于：外部知识库中蕴含的"程序性知识"（how-to 技能）无法被直接复用，每次任务都要重新推理，造成大量冗余计算和错误。

**2️⃣ 解决思路和核心技术**
Anything2Skill 提出了一套以"技能树"为先验的分类引导框架，将异构外部知识（文档、手册、日志、轨迹等）编译为可复用、可检索、可执行的结构化 Skill。整体流程分三阶段：**知识解析**——将每条知识记录分解为证据窗口，在技能树先验下进行 plan-and-expand 技能提取；**技能合约化**——将提取的技能候选转为结构化合约，包含调用条件、禁忌、动作序列、工作流步骤、约束、输出规范、支撑证据和置信度；**SkillBank 管理**——通过分类感知编译、注册级调和、生命周期追踪、版本化更新及技能树可视化，构建持久化程序性记忆库。推理时，Agent 同时从原始知识库检索陈述性证据（RAG）并从 SkillBank 检索相关程序性技能，实现"知识访问"与"能力复用"的双路增强。

**3️⃣ 主要实验结果**
在 qsv 和 GitHub-CLI 两个基准上，Anything2Skill + RAG 分别达到 **98.85%** 和 **94.10%** 的任务成功率，大幅优于纯 RAG Agent，验证了将潜在程序性知识编译为显式技能的有效性。

---

## MemToolAgent: Improving Agent Tool Use Through Memory Management

📂 方向：LLM Agent / 工具使用记忆增强
👤 作者：Suleyman Armagan Er, Danilo Ribeiro, Yogesh Virkar et al.
📅 日期：2026-06-09
🔗 PDF：https://arxiv.org/pdf/2606.07909
🔗 ArXiv：https://arxiv.org/abs/2606.07909

💡 **论文总结**：

**1️⃣ 解决的问题**
LLM Agent 在使用外部工具处理复杂任务时，面临无法从历史交互中学习的核心瓶颈。现有 Agent 对用户偏好和过去错误的记忆能力极为有限，每次会话都重新出发，既无法记住用户的特殊偏好（如时间格式要求），也无法避免重复犯同类错误，导致工具调用准确率和个性化程度不足。

**2️⃣ 解决思路和核心技术**
MemToolAgent 构建了一套无需 LLM 微调的记忆驱动工具使用框架，核心包含两个模块：**记忆提取模块**——将过去的 user-agent 对话历史处理为结构化记忆条目，采用基于反思（reflection）的提取机制，利用环境反馈和用户反馈将错误执行蒸馏为批评性记忆（critique entries）；**检索模块**——在推理时根据记忆相似度分布动态决定调用多少条历史经验，实现自适应记忆检索。所有记忆采用统一格式存储，既支持通用工具使用场景，也支持个性化偏好场景。整个框架不需要对底层 LLM 做任何微调，即插即用。

**3️⃣ 主要实验结果**
在三个基准测试上，MemToolAgent 相比强基线实现显著提升：WorkBench **+29%**、NESTFUL **+80%**、PEToolBench **+17%** 的相对性能提升，充分验证了记忆增强对 Agent 工具使用能力的有效性。
