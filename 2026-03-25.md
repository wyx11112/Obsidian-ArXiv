# 每日 ArXiv 论文推送 - 2026-03-25

> 搜索方向：RAG · Context Management · AI Agent · Agent Memory · Data Warehouse Agent

---

**EvoSchema: Towards Text-to-SQL Robustness Against Schema Evolution**
📂 方向：Data Warehouse Agent / Text-to-SQL
👤 作者：Tianshu Zhang, Kun Qian, Siddhartha Sahai et al.
📅 日期：2026-03-11
🔗 PDF：https://arxiv.org/pdf/2603.10697
🔗 ArXiv：https://arxiv.org/abs/2603.10697

💡 **论文总结**：

**1️⃣ 解决的问题**
现实世界中数据库 Schema 频繁演化（新增/删除列、表改名等），导致在静态 Schema 上训练的 Text-to-SQL 模型性能急剧下降。现有研究缺乏系统性、可控的 Schema 演化鲁棒性评测方法，无法真实反映企业级动态数据库环境。

**2️⃣ 解决思路和核心技术**
论文提出 EvoSchema，一个专门评估和提升 Text-to-SQL 系统在真实 Schema 变更下鲁棒性的综合基准。核心贡献包括：首先，构建了一套新颖的 Schema 演化分类体系，涵盖列级和表级两大维度共十种扰动类型（如列重命名、列合并、表拆分等），系统模拟真实数据库变化场景。其次，通过 EvoSchema 对多个开源与闭源 LLM 进行深度评测，发现表级扰动对模型性能的影响显著大于列级扰动——这一发现对数据库设计和模型训练均有重要指导意义。在提升鲁棒性方面，作者提出在训练数据中引入多样化 Schema 设计，强制模型学会区分相同问题在不同 Schema 下的差异，从而避免学习虚假的结构关联模式（spurious patterns）。该框架被 VLDB 2025 接收，代表了 Text-to-SQL 在动态企业环境适应性研究方面的重要进展。

**3️⃣ 主要实验结果**
在 EvoSchema 上，经过多样化 Schema 增强训练的模型相比未扰动数据训练的基线展现出显著更强的鲁棒性；表级扰动较列级扰动导致更大性能下降，证明 Schema 结构理解是关键瓶颈。

---

**MemCollab: Cross-Agent Memory Collaboration via Contrastive Trajectory Distillation**
📂 方向：Agent Memory
👤 作者：Yurui Chang, Yiran Wu, Qingyun Wu et al.
📅 日期：2026-03-24
🔗 PDF：https://arxiv.org/pdf/2603.23234
🔗 ArXiv：https://arxiv.org/abs/2603.23234

💡 **论文总结**：

**1️⃣ 解决的问题**
现有 LLM Agent 的记忆系统是"单 Agent 绑定"的：存储的知识与特定模型的推理风格深度耦合。在多异构 Agent 并存的现代部署场景中，简单地将一个 Agent 的记忆迁移给另一个 Agent 往往导致性能下降，因为记忆中混杂了任务相关知识与特定模型的偏置。

**2️⃣ 解决思路和核心技术**
MemCollab 提出了一种协作记忆框架，通过对比蒸馏构建"模型无关"的共享记忆。核心思路是：让不同 Agent 在同一任务上生成各自的推理轨迹，然后通过对比这些轨迹来蒸馏出"抽象推理约束"——这些约束捕捉不同模型间的任务层面共同不变量，同时抑制各模型的特有偏置。具体地，框架收集多个异构 Agent（包括跨模型族）在同类任务上的解题路径，通过对比学习识别哪些推理步骤是任务本质所需的、哪些是模型特有的风格。蒸馏出的约束形成一个可被任意 Agent 共享的记忆资源池。此外，论文引入任务感知检索机制，根据任务类别条件化记忆访问，确保推理时只调用相关约束，避免噪声干扰。

**3️⃣ 主要实验结果**
在数学推理和代码生成基准上，MemCollab 持续提升不同 Agent 的准确率和推理效率；在跨模型族设置（如不同架构的模型间共享记忆）中同样有效，验证了协作记忆作为共享推理资源的可行性。

---

**PCR: A Prefetch-Enhanced Cache Reuse System for Low-Latency RAG Serving**
📂 方向：RAG
👤 作者：Wenfeng Wang, Xiaofeng Hou, Peng Tang et al.
📅 日期：2026-03-24
🔗 PDF：https://arxiv.org/pdf/2603.23049
🔗 ArXiv：https://arxiv.org/abs/2603.23049

💡 **论文总结**：

**1️⃣ 解决的问题**
RAG 系统通过引入检索文档大幅增加了 LLM 的输入序列长度，导致 prefill 阶段（生成所有输入 token 的 KV 表示）计算成本激增，成为高并发服务场景下的主要延迟瓶颈。现有 KV-cache 复用方案受限于低缓存命中率、高 CPU-GPU 数据传输开销和慢速 SSD I/O 三大瓶颈，实际收益有限。

**2️⃣ 解决思路和核心技术**
PCR（Prefetch-Enhanced Cache Reuse）从系统工程角度出发，通过三项关键技术最大化 KV-cache 复用效率：（1）**前缀树缓存 + 前瞻 LRU 替换策略**：构建前缀树结构组织 KV 缓存，利用调度队列中的待处理请求做"前瞻"，预测哪些缓存块未来最可能被访问，从而优化缓存驱逐决策，大幅提升缓存命中率；（2）**层次化流水线重叠**：通过多个 CUDA Stream 将 KV-cache 的跨设备加载（CPU→GPU）与 GPU 计算并行化，以流水线方式隐藏通信延迟；（3）**基于队列的预取机制**：在 KV 数据被实际需要之前，主动将其从 SSD 预加载到 DRAM，消除 SSD 慢速 I/O 的阻塞。三者协同作用，在高吞吐场景下显著降低 Time-To-First-Token（TTFT）。

**3️⃣ 主要实验结果**
在多个数据集和主流 KV-cache 复用方法的对比实验中，PCR 实现最高达 **2.47x 的平均 TTFT 加速**，在高并发 RAG 服务场景中表现优于现有方案。
