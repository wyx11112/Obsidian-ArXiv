# ArXiv 每日论文推送 · 2026-04-17

---

## 论文一

**DPC: Training-Free Text-to-SQL Candidate Selection via Dual-Paradigm Consistency**

📂 方向：Data Warehouse Agent / Database Agent
👤 作者：Boyan Li, Ou Ocean Kun Hei, Yue Yu et al.
📅 日期：2026-04-16
🔗 PDF：https://arxiv.org/pdf/2604.15163
🔗 ArXiv：https://arxiv.org/abs/2604.15163

💡 **论文总结**：

**1️⃣ 解决的问题**
LLM 在生成 SQL 查询方面已有较强能力（Pass@K 高），但缺乏在无执行反馈的情况下自我评估正确性的能力，导致"生成-选择鸿沟"（Generation-Selection Gap）——即高潜力准确率无法转化为 Pass@1 的执行准确率。现有有监督验证器标注成本高且跨领域泛化差；无监督方法（如 Self-Consistency、LLM-as-a-Judge）则受到系统性偏差（幻觉共识）和符号盲区（无法模拟执行状态）的制约。

**2️⃣ 解决思路和核心技术**
论文提出 **DPC（Dual-Paradigm Consistency）**，一种免训练的多智能体框架，将 SQL 候选选择从"基于隐藏数据的概率猜测任务"重新定义为"基于可见数据的确定性验证任务"。核心架构包含三个协作 Agent：**SLICER** 负责从候选 SQL 之间的逻辑差异出发，构造一个最小可区分数据库（MDD，Minimal Distinguishing Database）——这是一个专门设计的对抗性微型可观测环境，用于暴露候选 SQL 之间的逻辑差异；**TESTER** 协助完善 MDD 的构建；**SOLVER** 则通过将候选 SQL 的执行结果与并行生成的 Python/Pandas 命令式解法进行交叉核验，利用声明式（SQL）与命令式（Python）两种范式之间的执行一致性来判断正确逻辑，从而有效规避单一范式的系统性幻觉问题。整个流程无需任何标注训练数据，仅依赖 LLM 的代码生成与推理能力即可运行。

**3️⃣ 主要实验结果**
在 BIRD 和 Spider 基准上，DPC 相对 Self-Consistency 等强基线实现绝对准确率提升最高 2.2%，在多个 LLM（包括 GPT-4 系列）上一致优于现有无监督选择方法。论文已被 ACL 2026 Main Track 接收。

---

## 论文二

**UniDoc-RL: Coarse-to-Fine Visual RAG with Hierarchical Actions and Dense Rewards**

📂 方向：RAG (Retrieval-Augmented Generation)
👤 作者：Jun Wang, Shuo Tan, Zelong Sun et al.
📅 日期：2026-04-16
🔗 PDF：https://arxiv.org/pdf/2604.14967
🔗 ArXiv：https://arxiv.org/abs/2604.14967

💡 **论文总结**：

**1️⃣ 解决的问题**
现有视觉 RAG 系统依赖通用检索信号，忽略了复杂推理所需的细粒度视觉语义——即检索出的文档往往包含大量与问题无关的视觉内容，导致大型视觉语言模型（LVLM）在需要精准视觉定位的推理任务中准确率受限。核心挑战在于如何让模型从粗粒度的文档检索逐步精细化到具体的图像区域，同时支持端到端训练。

**2️⃣ 解决思路和核心技术**
论文提出 **UniDoc-RL**，一个统一强化学习框架，让 LVLM Agent 在单一框架内联合执行检索（Retrieval）、重排序（Reranking）、主动视觉感知（Active Visual Perception）和推理（Reasoning）四项任务。核心设计是将视觉信息获取建模为序列决策问题，配备分层动作空间：Agent 从粗粒度文档检索开始，逐步精细化到图像选择，再到主动区域裁剪（Active Region Cropping），逐层压缩无关内容、聚焦信息密集区域。为支持端到端训练，论文引入密集多奖励方案（Dense Multi-Reward Scheme），对每个动作步骤均提供任务感知的监督信号。训练算法基于 GRPO（Group Relative Policy Optimization），无需单独的价值网络即可对齐多个优化目标。此外，论文还构建了包含高质量推理轨迹和细粒度动作标注的训练数据集。

**3️⃣ 主要实验结果**
在三个基准测试上，UniDoc-RL 相比现有基于 RL 的方法取得最高 17.7% 的性能提升，一致超越所有 state-of-the-art 基线。

---

## 论文三

**GAM: Hierarchical Graph-based Agentic Memory for LLM Agents**

📂 方向：Agent Memory
👤 作者：Zhaofen Wu, Hanrong Zhang, Fulin Lin et al.
📅 日期：2026-04-14
🔗 PDF：https://arxiv.org/pdf/2604.12285
🔗 ArXiv：https://arxiv.org/abs/2604.12285

💡 **论文总结**：

**1️⃣ 解决的问题**
LLM Agent 在维持长期连贯交互时，面临新信息获取与已有知识保留之间的矛盾：基于统一流式的记忆系统（Unified Stream-based Memory）虽然方便上下文更新，但容易受到短暂噪声的干扰；而离散结构化记忆架构（Discrete Structured Memory）虽然知识保留稳健，却难以适应动态演化的对话叙事。如何同时实现快速上下文感知和稳定长期知识保留，是 Agent Memory 领域的核心难题。

**2️⃣ 解决思路和核心技术**
论文提出 **GAM（Graph-based Agentic Memory）**，一个分层图结构 Agent 记忆框架，核心思想是**显式解耦记忆编码与记忆整合**过程。架构分为两层：底层为**事件演进图（Event Progression Graph）**，专门存储当前进行中的对话内容，隔离短暂噪声；上层为**主题关联网络（Topic Associative Network）**，仅在检测到语义主题转移时才将底层事件图的内容整合进来，从而在保持长期一致性的同时最小化跨对话干扰。在检索层面，GAM 引入图引导的多因素检索策略（Graph-Guided Multi-Factor Retrieval），基于图结构的拓扑关系和语义相关度联合提升上下文精准度，而非简单的向量相似度搜索。

**3️⃣ 主要实验结果**
在 LoCoMo 和 LongDialQA 两个长对话基准上，GAM 在推理准确率和效率两个维度均一致优于 state-of-the-art 基线。论文未公开具体指标数字。
