# 每日 ArXiv 论文推送 — 2026-05-26

---

## 论文一

**Knowledge Distillation for Low-Resource Open-source Text-to-SQL Model**

📂 方向：Text-to-SQL / Data Warehouse Agent
👤 作者：Tianhao Qiu, Xiaojun Chen et al.
📅 日期：2026-05-25
🔗 PDF：https://arxiv.org/pdf/2605.22843
🔗 ArXiv：https://arxiv.org/abs/2605.22843

💡 **论文总结**：

**1️⃣ 解决的问题**
在真实场景中，Text-to-SQL 系统常面临低资源困境：高质量标注的 `<question, SQL>` 对极为稀缺，尤其是领域专属数据库场景。同时，数据库 schema 中存在大量不透明的缩写定义和隐式业务逻辑，无法从 schema 结构本身推断，导致通用方法在领域专属任务中泛化能力严重不足。

**2️⃣ 解决思路和核心技术**
论文提出一套知识感知的 Text-to-SQL 框架（Knowledge-Aware Framework），核心思路是主动构建任务专属知识库，将 schema 语义、缩写词典、业务逻辑规则和常用查询模式进行结构化组织，并在训练与推理两个阶段分别注入。

在训练阶段，框架利用上述知识库生成多样化、语境一致的合成训练数据，使得数据不仅覆盖面广，还与数据库约束语义对齐。在推理阶段，通过针对性的知识检索（targeted knowledge retrieval），将与当前问题最相关的知识片段动态拼接到 prompt 中，辅助模型生成正确 SQL。

整体架构融合了知识蒸馏（Knowledge Distillation）思想，将大模型的推理能力迁移至开源小模型，同时通过结构化知识注入弥补小模型在领域理解上的短板。这套流程在七个基准数据集上进行了验证，既包含通用数据集（如 Spider），也包含多个领域专属数据集，覆盖开源与闭源 LLM 多种场景。

**3️⃣ 主要实验结果**
在七个基准（通用 + 领域专属）上均取得显著提升，尤其在低资源领域专属场景下表现突出，增强了模型泛化性与鲁棒性；论文未公开具体指标数字。

---

## 论文二

**MemAudit: Post-hoc Auditing of Poisoned Agent Memory via Causal Attribution and Structural Anomaly Detection**

📂 方向：Agent Memory / 安全审计
👤 作者：Zhewen Tan, Yilun Yao, Huiyan Jin et al.
📅 日期：2026-05-25
🔗 PDF：https://arxiv.org/pdf/2605.23723
🔗 ArXiv：https://arxiv.org/abs/2605.23723

💡 **论文总结**：

**1️⃣ 解决的问题**
LLM Agent 通常依赖持久化记忆库存储历史交互、检索相关示例以支持长程任务执行。但这一机制带来安全漏洞：攻击者可通过正常交互向记忆库注入恶意记录，后续被检索时引导 Agent 产生有害行为。现有防御手段聚焦于在线干预（如 prompt 过滤），无法回答"哪些记忆导致了已发生的有害行为"这一事后溯源问题。

**2️⃣ 解决思路和核心技术**
论文提出 **MemAudit**，一个针对记忆增强型 LLM Agent 的事后因果记忆审计框架，结合两类互补信号完成有毒记忆识别：

第一，**反事实记忆影响分数（Counterfactual Memory Influence Score）**：对记忆库中每条记录，通过反事实推理度量其对有害输出的因果贡献度——即移除该记录后有害输出的变化幅度，从而量化每条记忆的"罪责"。

第二，**记忆一致性图（Memory Consistency Graph）**：将记忆库建模为结构图，通过检测图中异常节点（结构性孤立或语义不一致的记忆）识别投毒记录，作为对因果打分的结构补充。

两种信号联合判断，实现对有毒记忆的精准定位，无需修改 Agent 运行时流程，适用于 QA 和推理 Agent 多种场景。针对 MINJA 攻击（仅通过正常交互注入、不直接修改记忆库的攻击方式）进行评估。

**3️⃣ 主要实验结果**
QA 场景攻击成功率从 **70%** 降至 **0%**；RAP 攻击成功率从 **83.3%** 降至 **0%**，完全消除投毒攻击效果。

---

## 论文三

**Parallel Context Compaction for Long-Horizon LLM Agent Serving**

📂 方向：Context Management / AI Agent
👤 作者：Musa Cim, Burak Topcu, Chita Das et al.
📅 日期：2026-05-25
🔗 PDF：https://arxiv.org/pdf/2605.23296
🔗 ArXiv：https://arxiv.org/abs/2605.23296

💡 **论文总结**：

**1️⃣ 解决的问题**
长程 LLM Agent 在持续运行中会积累不断增长的对话历史，最终超出模型上下文窗口限制。现有方案采用基于 LLM 的摘要压缩（summarization compaction），但存在三个核心问题：（1）摘要过程本身有损，丢失关键信息；（2）摘要调用会阻塞 Agent 推理数十秒；（3）运营方无法精确控制摘要体量，导致 Agent 跨次运行保留的知识不可预测。

**2️⃣ 解决思路和核心技术**
论文提出**并行上下文压缩（Parallel Context Compaction）**方法，将上下文压缩从串行阻塞调用改造为与 Agent 主推理流程并行执行，从根本上消除压缩带来的延迟停顿。

具体架构上，将待压缩的上下文切分为若干独立块（block），各块并行进行摘要生成，摘要结果异步写回，不阻断当前 Agent 推理。由于每块独立处理，运营方可以对每块分别应用精细化的 prompt 工程，实现对摘要体量和保留内容的精确可预测控制，解决了整体摘要中 prompt 指令被忽视的问题。

实验覆盖 8B 至 120B 参数的四种主流骨干模型（dense 与 MoE 架构，推理与非推理模型），在 HotpotQA 多跳问答和 LoCoMo 长对话两个基准上与串行基线对比。

**3️⃣ 主要实验结果**
在匹配压缩解码量的条件下，并行压缩显著降低端到端墙钟时间（wall time）并提升压缩吞吐量，同时提供更可预测的摘要体量控制；论文未公开具体数字指标。
