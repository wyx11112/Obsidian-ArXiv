## CoTE-SQL: 自增强微调融合推理与泛化，Text-to-SQL 新 SOTA

📂 方向：Text-to-SQL / 自然语言接口 / LLM 微调
👤 作者：Feng Lyu, Jinfeng Cen, Sijing Duan et al.
📅 日期：2026-06-16
🔗 PDF：https://arxiv.org/pdf/2606.15598
🔗 ArXiv：https://arxiv.org/abs/2606.15598

💡 **论文总结**：

**1️⃣ 解决的问题**
现有基于 LLM 的 Text-to-SQL 方法在推理能力与泛化能力之间难以兼顾：推理强的模型在新 schema 上容易过拟合，而泛化好的模型在复杂查询上推理深度不足。如何在无需人工标注的前提下，让开源 LLM 同时具备强推理和跨域泛化，是本文要解决的核心问题。

**2️⃣ 解决思路和核心技术**
论文提出 **CoTE-SQL**（Chain-of-Thought Enhanced SQL），围绕三项创新构建：

① **自增强推理链蒸馏**：利用 LLM 自身生成高质量推理轨迹（reasoning traces），无需人工标注即可构造监督信号，并通过自我过滤机制保留执行正确的样本作为微调数据。

② **结构化 CoT 提示与模块化分解**：将 Text-to-SQL 分解为 schema linking → SQL 骨架生成 → 条件填充等模块，每个模块配合 few-shot 样例检索，引导模型逐步推理，降低单步复杂度。

③ **执行时错误感知修订**：将生成的 SQL 在数据库上实际执行，捕获语法错误和执行错误后反馈给模型进行自我修订，实现 test-time 的错误修复闭环。三项机制协同作用，使模型在学习阶段获得更强推理能力，在推理阶段通过动态反馈进一步提升准确率。

**3️⃣ 主要实验结果**
在 Bird 基准上达到 53.39% EX / 59.02 VES（开源同量级 LLM SOTA）；Spider 基准上达到 79.60% EX / 77.19 VES，复杂查询提升尤为显著。

---

## CoeusBI：百度落地的 LLM 驱动交互式商业智能系统

📂 方向：LLM + BI / Text-to-SQL / 数据仓库智能体
👤 作者：Jinqing Lian, Chaofan Li, Yingxia Shao et al.
📅 日期：2026-06-16
🔗 PDF：https://arxiv.org/pdf/2606.15384
🔗 ArXiv：https://arxiv.org/abs/2606.15384

💡 **论文总结**：

**1️⃣ 解决的问题**
工业级交互式 BI 系统面临三大核心痛点：(a) 多表频繁 JOIN 导致 SQL 生成准确率下降；(b) 超宽 Schema（列数极多）使 schema linking 极其困难；(c) 跨方言查询生成和多轮对话支持带来高计算成本和低准确率。商业 BI 产品虽引入自然语言接口，但仍依赖大量人工配置语义层，无法自适应复杂业务场景。

**2️⃣ 解决思路和核心技术**
CoeusBI 提出**双 Agent 架构 + 层次化 Schema Linking** 的整体设计：

① **离线视图生成 Agent（View Generation Agent）**：在离线阶段自主将复杂 JOIN 查询转换为单视图查询，借助错误反馈迭代优化，彻底消除在线查询时的 JOIN 复杂度，无需人工语义建模。

② **层次化 Schema Linking 模块**：对超宽 Schema 采用向量检索分层筛选视图和字段，极大降低无关列的干扰，提升 schema 对齐精度，支持千列级别的宽表场景。

③ **动态路由 Agent（Routing Agent）**：在线阶段动态评估多轮对话上下文，决策是新生成中间表示还是修改已有表示，最终通过方言无关的确定性 SQL 编译器统一输出，实现对不同数据库方言的零额外适配成本。

**3️⃣ 主要实验结果**
在公开数据集和百度生产数据集上均显著超越现有方法，已在百度数据平台作为独立服务上线，日均服务数千用户，跨多条业务线稳定运行。论文未公开具体指标数字。

---

## TEND + SAG：首个面向 NoSQL 文档库的 Text-to-NoSQL 基准与求解器

📂 方向：Text-to-NoSQL / 非结构化数据库查询生成 / Schema-less 推理
👤 作者：Jinwei Lu, Jiawei Lu, Chen Zhang et al.
📅 日期：2026-06-16
🔗 PDF：https://arxiv.org/pdf/2502.11201
🔗 ArXiv：https://arxiv.org/abs/2502.11201

💡 **论文总结**：

**1️⃣ 解决的问题**
NoSQL 数据库（如 MongoDB）是核心数据基础设施，但自然语言访问能力严重滞后：无模式文档存储中存在嵌套路径、数组、缺失字段、动态键等结构，现有 NL2SQL 能力无法直接迁移。缺乏专门的 Text-to-NoSQL 基准，导致该方向的研究无从评估、无法推进。

**2️⃣ 解决思路和核心技术**
论文双管齐下，同时提出数据集与求解器：

① **TEND 基准（Text-to-NoSQL Dataset）**：包含 1,210 条 MongoDB 原生任务，跨 11 个数据库，由专家手工定义集合边界、嵌套数组、稀疏路径、多态结构和动态键约定，数据库以真实数据填充并通过 MongoDB 冻结执行验证。这是首个真正面向 schema-less 文档推理的基准，而非 SQL-to-MQL 迁移的变体。评测指标采用有界列容错执行准确率（EXC）+ 结果集 F1 + 执行结果分解三维度。

② **SAG 求解器（Schema-as-Data Grounding）**：核心创新在于将 schema 发现转化为数据驱动的路径和值接地（grounding）过程——从已存储的文档证据中归纳出合法路径与取值，再进行有界 MQL 生成、执行引导的错误修复，以及结果一致性筛选，避免对预定义 schema 的依赖。

**3️⃣ 主要实验结果**
实验表明，在 NL2SQL 上表现强劲的 LLM 在 TEND 上性能显著下降，验证了 Text-to-NoSQL 是独立于 Text-to-SQL 的新问题。论文未公开具体指标数字，但以对比实验形式揭示了该任务的挑战性。
