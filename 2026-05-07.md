# 每日 ArXiv 论文推送 · 2026-05-07

---

## 论文一

**FINER-SQL: Boosting Small Language Models for Text-to-SQL**
📂 方向：Text-to-SQL / NL2SQL
👤 作者：Thanh Dat Hoang, Thanh Trung Huynh, Matthias Weidlich et al.
📅 日期：2026-05-06
🔗 PDF：https://arxiv.org/pdf/2605.03465
🔗 ArXiv：https://arxiv.org/abs/2605.03465

💡 **论文总结**：

**1️⃣ 解决的问题**
大型语言模型在 Text-to-SQL 任务上表现优秀，但计算成本高、推理延迟大且存在数据隐私隐患，难以在实际生产环境中落地。小型语言模型（SLM）虽可本地部署、保护隐私，但推理能力弱、指令跟随差。现有基于稀疏二元奖励（0/1）的强化学习方法，在 SQL 生成错误时反馈信号极弱，导致训练不稳定乃至崩溃。

**2️⃣ 解决思路和核心技术**
FINER-SQL 提出了一套可扩展、可复用的强化学习框架，基于 **Group Relative Policy Optimization (GRPO)** 对 SLM 进行微调，核心创新在于用「细粒度执行反馈」取代稀疏二元奖励。框架引入两类关键奖励函数：

- **Memory Reward（记忆奖励）**：将模型推理过程与经验证的推理链（verified traces）对齐，保证语义稳定性，抑制推理漂移。
- **Atomic Reward（原子奖励）**：在操作层面（如 SELECT / JOIN / WHERE 子句）衡量生成 SQL 与目标 SQL 的结构重叠度，为「结构正确但不完整」的 SQL 给予部分学分，而非直接判零分。

这两种奖励将离散的正确性判断转化为连续的学习信号，无需额外的 Critic 网络，训练更稳定。整个框架无需人工标注新数据，具有良好的数据可复用性与扩展性。

**3️⃣ 主要实验结果**
在 BIRD 和 Spider 基准上，3B 参数模型分别达到 **67.73%** 和 **85%** 执行准确率，推理延迟仅 **5.57 s/sample**，性能媲美更大规模 LLM。

---

## 论文二

**What Happens Inside Agent Memory? Circuit Analysis from Emergence to Diagnosis**
📂 方向：Agent Memory / LLM Interpretability
👤 作者：Xutao Mao, Jinman Zhao, Gerald Penn et al.
📅 日期：2026-05-06
🔗 PDF：https://arxiv.org/pdf/2605.03354
🔗 ArXiv：https://arxiv.org/abs/2605.03354

💡 **论文总结**：

**1️⃣ 解决的问题**
基于 LLM 的 Agent 在记忆操作（写入、管理、读取）失败时往往表现「静默」——它会生成流畅的回答，却实际丢失了跨会话所需的关键信息。现有研究只描述了外部 write-manage-read 流程，尚不清楚这些阶段由模型内部哪些计算电路实现，也没有方法在不监督的情况下定位具体失败发生在哪一阶段。

**2️⃣ 解决思路和核心技术**
本文对 Qwen-3 系列（0.6B–14B）结合两个主流记忆框架（mem0 和 A-MEM）进行 **内部特征电路追踪（Circuit Analysis）**，系统研究记忆能力的涌现与可干预性。主要发现三条：

1. **控制先于内容涌现**：路由电路（控制组）在 0.6B 已可被因果性检测到，而内容电路在本实验设置下要到 4B 才有可检测信号。这意味着小模型「看起来会路由」却在提取和接地阶段静默失败，形成一个危险的虚假能力区间。

2. **Write 与 Read 共享晚层中枢**：内容组中，写入和读取操作共用一个「上下文接地基底（context-grounding substrate）」，该基底在 base 模型中已存在；只有显式的记忆框架才能在此基底上激活功能性接地方向，且该中枢可在两个框架间迁移。

3. **涌现 ≠ 可操控**：内容电路在 4B 可被检测，但要到 **8B** 才能被可靠干预，说明「可见性阈值」与「可控性阈值」是两个不同的规模节点。

基于上述发现，论文提出利用两组电路在特征空间的可分性，实现**无监督的逐操作失败定位**，无需人工标注即可诊断 Agent 记忆在哪个阶段出错。

**3️⃣ 主要实验结果**
无监督逐操作失败定位准确率达 **76.2%**，验证了特征电路分析作为 Agent 记忆诊断工具的实用价值。

---

## 论文三

**MEMTIER: Tiered Memory Architecture and Retrieval Bottleneck Analysis for Long-Running Autonomous AI Agents**
📂 方向：Agent Memory / Long-Context Retrieval
👤 作者：Bronislav Sidik, Lior Rokach
📅 日期：2026-05-06
🔗 PDF：https://arxiv.org/pdf/2605.03675
🔗 ArXiv：https://arxiv.org/abs/2605.03675

💡 **论文总结**：

**1️⃣ 解决的问题**
长期运行的自主 AI Agent（连续运行 72 小时以上）在现有平面文件记忆系统中存在严重的「记忆一致性问题」：工具执行成功率随时间下降 14 个百分点，根因是四种相互叠加的失败模式（提取失败、语义漂移、检索噪声、跨会话失忆）。

**2️⃣ 解决思路和核心技术**
MEMTIER 为 Agent 运行时设计了一套**三层记忆架构**，各层职责明确：

- **情节层（Episodic Tier）**：基于结构化 JSONL 存储，记录原始事件序列，支持时序查询。
- **语义层（Semantic Tier）**：由异步巩固守护进程（consolidation daemon）负责将情节层事实提升到语义层，实现长期知识沉淀。
- **检索引擎**：五信号加权检索引擎，综合多维度相关性信号；并引入基于注意力归因的**认知权重更新循环（cognitive weight update loop）**，动态调整各信号权重；同时结合 **PPO（近端策略优化）**框架自适应优化检索策略。

整套架构设计目标是可在消费级 6GB GPU 笔记本本地运行，兼顾性能与资源约束。

**3️⃣ 主要实验结果**
在 LongMemEval-S 完整 500 题基准（Qwen2.5-7B，消费级 6GB GPU）上：准确率 **Acc=0.382，F1=0.412**，相比全上下文基线提升 **+33 百分点**（0.050→0.382）。结合 DeepSeek-V4-Flash 预填充，单会话召回率达 **0.686–0.714**，超越论文中 RAG BM25 + GPT-4o 基线（0.560）。
